{
  "name": "overunderrated",
  "version": "1.0.0",
  "private": true,
  "description": "Crowd-rated toplist of the most overrated and underrated things.",
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node --no-warnings=ExperimentalWarning local-server.js",
    "start": "node --no-warnings=ExperimentalWarning local-server.js",
    "test": "node --no-warnings=ExperimentalWarning --test test/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
